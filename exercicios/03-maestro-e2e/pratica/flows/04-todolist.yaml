# Flow 4 — Adicionar item na todo list e verificar que aparece
# Usa env para parametrizar o texto do item (padrão Maestro para dados reutilizáveis)
appId: com.apptestesmobile
env:
  TODO_ITEM: "Comprar leite"
---
- launchApp
# TODO: navegar até a todo list
# TODO: tapOn { id: "todolist-add-button" }
- inputText: ${TODO_ITEM}         # env parametriza o texto — tente mudar o valor acima
# TODO: assertVisible do item recém-adicionado (use o texto ou todolist-item-${id})
